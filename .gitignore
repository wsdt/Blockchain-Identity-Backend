# Binaries for programs and plugins
*.dll
*.so
*.dylib

# PUSH EXE and LINUX BINARIES

# do not push secrets
_config/
.idea/

# do not push dependencies
vendor/

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
